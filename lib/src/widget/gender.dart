import 'package:flutter/material.dart';

class GendersScreen extends StatelessWidget {
  const GendersScreen({Key? key}) : super(key: key);
  
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('GendersScreen'),
      ),
    );
  }
}